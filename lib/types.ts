export type Prompt = {
  id: string;
  title: string;
  description: string;
  promptText: string;
  category: string;
  image: string;
};

export type Category = {
  id: string;
  name: string;
  blurb: string;
  image: string;
};
